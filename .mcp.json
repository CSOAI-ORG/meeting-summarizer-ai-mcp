{
  "name": "meeting-summarizer-ai-mcp",
  "description": "Meeting Summarizer Ai MCP server. Tools: summarize meeting, extract action items, identify decisions. Built by MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "summarize_meeting",
      "description": "Summarize a meeting transcript into key points, topics discussed, and participant info.",
      "parameters": {
        "type": "object",
        "properties": {
          "transcript": {
            "type": "string"
          },
          "max_sentences": {
            "type": "integer"
          }
        },
        "required": [
          "transcript"
        ]
      }
    },
    {
      "name": "extract_action_items",
      "description": "Extract action items and tasks from meeting transcript with assignee detection.",
      "parameters": {
        "type": "object",
        "properties": {
          "transcript": {
            "type": "string"
          }
        },
        "required": [
          "transcript"
        ]
      }
    },
    {
      "name": "identify_decisions",
      "description": "Identify key decisions made during a meeting from the transcript.",
      "parameters": {
        "type": "object",
        "properties": {
          "transcript": {
            "type": "string"
          }
        },
        "required": [
          "transcript"
        ]
      }
    },
    {
      "name": "generate_followup",
      "description": "Generate a follow-up email draft from a meeting transcript.",
      "parameters": {
        "type": "object",
        "properties": {
          "transcript": {
            "type": "string"
          },
          "meeting_title": {
            "type": "string"
          },
          "recipients": {
            "type": "array"
          }
        },
        "required": [
          "transcript"
        ]
      }
    }
  ]
}